<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 http://maven.apache.org/xsd/settings-1.0.0.xsd">

    <localRepository>{{{localRepositoryPath}}}</localRepository>
    <interactiveMode />
    <usePluginRegistry />
    <offline />
    <pluginGroups />

    <proxies>
        <proxy>
            <id>proxy</id>
            <active>{{{proxyActive}}}</active>
            <protocol>{{{proxyProtocol}}}</protocol>
            <host>{{{proxyHostname}}}</host>
            <port>{{{proxyPort}}}</port>
            <username>{{{proxyUsername}}}</username>
            <password>{{{proxyPassword}}}</password>
            <nonProxyHosts></nonProxyHosts>
        </proxy>
    </proxies>

    <servers>

        <server>
            <id>prj-mdk-releases</id>
            <username>{{{mdk_login}}}</username>
            <password>{{{mdk_password}}}</password>
        </server>

        <server>
            <id>prj-mdk-plugin-releases</id>
            <username>{{{mdk_login}}}</username>
            <password>{{{mdk_password}}}</password>
        </server>

    </servers>

    <profiles>
        <profile>
            <id>mdk</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <android.sdk.path>{{{androidSdkPath}}}</android.sdk.path>
            </properties>
            <repositories>
                <repository>
                    <id>prj-mdk-releases</id>
                    <name>prj-mdk-releases</name>
                    <layout>default</layout>
                    <url>{{{mdkRepoRelease}}}</url>
                    <snapshots>
                        <enabled>false</enabled>
                    </snapshots>
                    <releases>
                        <enabled>true</enabled>
                    </releases>
                </repository>
            </repositories>

            <pluginRepositories>
                <pluginRepository>
                    <id>prj-mdk-plugin-releases</id>
                    <name>prj-mdk-plugin-releases</name>
                    <layout>default</layout>
                    <url>{{{mdkRepoPluginRelease}}}</url>
                    <snapshots>
                        <enabled>false</enabled>
                    </snapshots>
                    <releases>
                        <enabled>true</enabled>
                    </releases>
                </pluginRepository>
            </pluginRepositories>
        </profile>
    </profiles>

</settings>